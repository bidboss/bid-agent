1. AGENTS.md 作为用户的系统上下文
2. front/AGENTS.md 的预期格式是什么？这决定了如何解析
makdown + yaml formatter / 纯文本  / 分章节
3. AGENTS.md 加载后的处理方式?
整体拼进 SystemMessage
4. 现有 src/tools/ 下的 JS 工具是否一起迁移到 TS？
5. 使用 ChatPromptTemplate.fromMessages 构建提示模板，替换手动字符串拼接?
LangChain.js 原生就有 `ChatPromptTemplate`，正是用来做这件事的，优先用它，而不是手动字符串拼接，这是 LangChain 标准推荐写法。好处很多（不用自己拼接，自动生成 BaseMessage ，动态更新）

6. 工具注册中心中提供的 listTools 直接提供 兼容openai 格式的工具 vs langchain 提供的标准工具定义格式 ToolDefinition？
不行，这种方式耦合强，扩展性不足。
后面换对接模型，只要不是 openai 格式，就需要该 listtools 的代码。
应该使用 langchain 提供的标准工具定义格式 ToolDefinition 。各 模型provider方 在自己实现的 bindTools() 里负责转换。

7. MCP 工具命名要不要带服务器前缀？
需要，防止和本地 或者其他地方的工具名重复，冲突。

8. 启动时 MCP 服务器加载失败，应该如何处理？
本地工具同步加载，第三方工具异步加载，加载完成自动追加，如果加载失败，也不阻塞其他工具的加载

9. MCP 工具返回 content[] 是结构化数组（含 text/image/resource 等），怎么处理成 ToolResult.content 字符串？

10. 长期记忆的存储介质：使用 .md 长期 + LanceDB 向量记忆

11. 把对话转换成长期记忆的触发策略，触发时机？
指令 + LLM 增量自动总结（兼容，双管齐下）

12. 记忆需要自维护、增量合并，而不是 全量覆盖

13. LanceDB 向量记忆 和 RAG 文档库怎么分？
复用同一个库、不同表名

14. 短期记忆窗口使用哪种策略？
按轮次\按token数\按消息条数
消息长短

15. 当前的短期记忆有问题：
旧记忆逻辑会在token超限后追加摘要消息到对话列表。摘要本身占用token，下一轮对话极易再次超限，反复触发总结，形成死循环；同时摘要存入session快照，前端展示混入总结文本，污染原始对话记录。

需要重新设计：
整体方案：运行时临时摘要，摘要不持久化

1 只持久化原始问答，摘要不存入消息数组、不写文件、前端不展示，仅本次LLM调用临时生成，调用完直接丢弃。
2.读取全部原始历史，使用countTokens计算总token。没超过就 [system,全部原始历史] ，不生成摘要。
超过最大token数：取出最古老的多组完整对话轮（Human+AI成对，不切半轮），调用LLM生成摘要，临时候选数组移除这批原始对话，在system后插入最多1条临时摘要消息。[系统上下文+第一对用户、助手消息 + 摘要]。
3.约束：裁剪、摘要仅在临时组装上下文阶段执行，不修改json里的原始对话。用户消息 和 AI消息，工具消息，正常往json中添加。

预期最终效果：
日常对话token充足时，完全使用原始问答上下文，无摘要、无额外损耗；当对话变长触发阈值，自动把老旧对话压缩为临时摘要送入模型，控制token不超限。用户全程只能看到完整原始一问一答，看不到任何摘要文本；不会出现反复循环总结、多条摘要叠加的问题。会话重启后摘要消失，重新从原始对话开始。

特别说明：
当前临时摘要不持久化；后续长会话场景再升级为独立字段持久摘要。

