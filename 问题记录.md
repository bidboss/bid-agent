1. 保存在session 会话记录里面没有 run_id，但是调用模型时是会生成的。
run_id 表示一次大模型调用，从消息发出到返回消息。
message_id 用来表示一个消息。

2. 大模型返回的 AIMessage 中 addtional_kwargs 字段里有数据，但是序列化以后，变成空对象了。
3. 是否需要存入工具消息 ？
需要，不然 模型会失忆，多轮工具调用，会忘掉之前的工具调用。同时 如果不存，历史会话的消息中直接丢失 工具调用的记录，推理链不完整。
存 是业界的通用做法。